# Config Gen

collection all the config here, and generate corresponding config for each module:

- fastapi (backend)
- vite (frontend, auth)
- hugo (frontend, content)

Put configs to `./uni-conf/$env/xxx`. 

Those configs all will be git-ignored. But for example, we make an example dir in `./uni-conf/example` so you can based on these build your own setting.

